import Freezer from 'freezer-js';
import initialState from './initialState';

/**
 * [Freezer](https://github.com/arqex/freezer) object representing the app state.
 *
 * @module State
 */
export default new Freezer(initialState);
